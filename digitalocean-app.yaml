name: titi-invita
region: nyc
services:
- name: titi-invita-api
  github:
    branch: main
    deploy_on_push: true
  source_dir: /
  envs:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: "3000"
  # CONFIGURACIÓN DE TU BASE DE DATOS
  - key: DB_HOST
    value: "app-dcbdb4b2-d446-4cf7-bee6-40a00a634692-do-user-2165170-0.l.db.ondigitalocean.com"
  - key: DB_PORT
    value: "25060"
  - key: DB_NAME
    value: "dev-db-975741"
  - key: DB_USER
    value: "dev-db-975741"
  - key: DB_PASSWORD
    value: "AVNS_lRIHSLrwyt6XMZH4z1O"
  # JWT CONFIG
  - key: JWT_SECRET
    value: "tu_super_secreto_jwt_titi_invita_2026@!"
  - key: JWT_EXPIRES_IN
    value: "86400"
  # CORS
  - key: CORS_ORIGIN
    value: "https://titi-invita.ondigitalocean.app"
  
  http_port: 3000
  instance_count: 1
  instance_size_slug: basic-xxs
  build_command: npm install && npm run db:init && npm run db:seed
  run_command: npm start
  health_check:
    http_path: /api/health
    initial_delay_seconds: 120
    period_seconds: 30
    timeout_seconds: 10
    success_threshold: 1
    failure_threshold: 3
