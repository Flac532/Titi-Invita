name: titi-invita-app
region: nyc

services:
- name: titi-invita-api
  
  # Configuración del repositorio GitHub
  github:
    branch: main
    repo: tu-usuario/titi-invita
    deploy_on_push: true
  
  # Directorio raíz
  source_dir: /
  
  # Variables de entorno
  envs:
  - key: NODE_ENV
    value: production
  
  - key: PORT
    value: "8080"
  
  # PostgreSQL DigitalOcean
  - key: DB_HOST
    value: app-dcbdb4b2-d446-4cf7-bee6-40a00a634692-do-user-2165170-0.l.db.ondigitalocean.com
  
  - key: DB_PORT
    value: "25060"
  
  - key: DB_NAME
    value: dev-db-975741
  
  - key: DB_USER
    value: dev-db-975741
  
  - key: DB_PASSWORD
    value: AVNS_lRIHSLrwyt6XMZH4z1O
  
  # JWT Secret
  - key: JWT_SECRET
    value: TitiInvita2026SecretKey!ChangeThisInProduction
  
  # Configuración del servicio
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  
  # Comandos de build y ejecución
  build_command: |
    echo "🚀 Construyendo..."
    rm -f package-lock.json
    npm install --production
    echo "✅ Build completado"
  
  run_command: node server.js
  
  # Health check
  health_check:
    http_path: /api/health
    initial_delay_seconds: 30
    period_seconds: 15
    timeout_seconds: 10
    success_threshold: 1
    failure_threshold: 5
